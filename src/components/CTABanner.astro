---
/**
 * ─── CTABanner Component (DESACTIVADO) ───
 * Banner de CTA pre-footer. Desactivado a petición: no renderiza nada en todo
 * el sitio. Se conservan los tipos de Props para no romper las ~200 llamadas
 * existentes. Para reactivar, restaurar el markup desde el historial de git.
 */

type Props = {
  headline?: string;
  subheadline?: string;
  primaryCTA?: { label: string; href: string };
  secondaryCTA?: { label: string; href: string };
  variant?: "default" | "dark" | "accent";
};

// Props aceptadas pero no usadas (componente desactivado).
const _props = Astro.props as Props;
void _props;
---

{/* CTABanner desactivado — no renderiza contenido. */}
